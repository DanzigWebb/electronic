import { FolderSearchPipe } from './folder-search.pipe';

describe('FolderSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FolderSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
